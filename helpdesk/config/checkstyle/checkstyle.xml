<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  GovHelpDesk Checkstyle — derived from google_checks.xml
  =========================================================
  Base:    google_checks.xml (canonical, as supplied)
  Target:  Spring Boot 3.3 + Lombok + MapStruct + Maven

  CHANGES FROM GOOGLE BASE — every deviation is documented inline
  with the tag [CHANGED], [REMOVED], or [ADDED] so a diff is trivial.

  Summary of changes:
    [CHANGED] severity        warning → error  (CI must fail on violations)
    [CHANGED] fileExtensions  java,properties,xml → java only
    [CHANGED] LineLength.max  100 → 120
    [CHANGED] Indentation     2-space → 4-space (Spring Boot convention)
    [CHANGED] AbbreviationAsWordInName.allowedAbbreviationLength  0 → 2
    [CHANGED] MissingJavadocMethod — scope protected → private (less noisy)
    [CHANGED] MissingJavadocType  — scope protected → private (less noisy)
    [REMOVED] TextBlockGoogleStyleFormatting  (Checkstyle 10.18+, not in 10.12)
    [REMOVED] JavadocLeadingAsteriskAlign     (10.18+, not in 10.12)
    [REMOVED] JavadocMissingLeadingAsterisk   (10.18+, not in 10.12)
    [REMOVED] MissingOverrideOnRecordAccessor (10.14+, not in 10.12)
    [REMOVED] ConstructorsDeclarationGrouping (10.17+, not in 10.12)
    [REMOVED] GoogleNonConstantFieldName      (Google-internal, not in OSS distribution)
    [ADDED]   MemberName — replaces GoogleNonConstantFieldName
    [ADDED]   MethodLength max=50
    [ADDED]   CyclomaticComplexity max=15
    [ADDED]   BeforeExecutionExclusionFileFilter for generated sources and MapperImpl
-->
<module name="Checker">

    <property name="charset" value="UTF-8"/>

    <!--
      [CHANGED] Google default is "warning" controlled by a property.
      We hardcode "error" so the CI build fails on any violation.
      Rationale: a warning that never breaks the build is not enforced.
    -->
    <property name="severity" value="error"/>

    <!--
      [CHANGED] Google checks java, properties, xml.
      We check java only — properties and XML are linted by other tools
      (Spotless for formatting, Spring's own relaxed-binding for structure).
    -->
    <property name="fileExtensions" value="java"/>

    <!-- Exclude module-info.java — unchanged from Google base -->
    <module name="BeforeExecutionExclusionFileFilter">
        <property name="fileNamePattern" value="module\-info\.java$"/>
    </module>

    <!--
      [ADDED] Exclude Lombok-generated and MapStruct-generated sources.
      BeforeExecutionExclusionFileFilter matches on the file path before
      any TreeWalker check runs — the cleanest exclusion mechanism because
      it does not require per-module suppression. Hand-written code in
      src/main/java is still fully checked.
    -->
    <module name="BeforeExecutionExclusionFileFilter">
        <property name="fileNamePattern" value="[\\/]generated(-sources)?[\\/].*\.java$"/>
    </module>
    <module name="BeforeExecutionExclusionFileFilter">
        <property name="fileNamePattern" value=".*MapperImpl\.java$"/>
    </module>

    <!-- Unchanged from Google base -->
    <module name="SuppressWarningsFilter"/>

    <!-- Unchanged from Google base — optional suppressions file -->
    <module name="SuppressionFilter">
        <property name="file" value="${org.checkstyle.google.suppressionfilter.config}"
                  default="checkstyle-suppressions.xml"/>
        <property name="optional" value="true"/>
    </module>

    <!-- Unchanged from Google base -->
    <module name="SuppressWithNearbyTextFilter">
        <property name="nearbyTextPattern"
                  value="CHECKSTYLE.SUPPRESS\: (\w+) for ([+-]\d+) lines"/>
        <property name="checkPattern" value="$1"/>
        <property name="lineRange" value="$2"/>
    </module>

    <!-- Unchanged from Google base — no tabs anywhere -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <!--
      [CHANGED] max 100 → 120.
      Google uses 100; Spring Boot annotations (@DynamicPropertySource,
      long @Query strings, JWT configuration) make 100 impractical without
      artificial line breaks that reduce, not increase, readability.
      ignorePattern: unchanged from Google base (package, import, URLs).
    -->
    <module name="LineLength">
        <property name="fileExtensions" value="java"/>
        <property name="max" value="120"/>
        <property name="ignorePattern"
                  value="^package.*|^import.*|href\s*=\s*&quot;[^&quot;]*&quot;|http://|https://|ftp://"/>
    </module>

    <!-- Unchanged from Google base — text block line-length suppression -->
    <module name="SuppressWithPlainTextCommentFilter">
        <property name="checkFormat" value="LineLength"/>
        <property name="offCommentFormat" value='^.*"""\s*$'/>
        <property name="onCommentFormat" value='^\s*"""\s*(?:[,;]|.+)$'/>
    </module>
    <module name="SuppressWithPlainTextCommentFilter">
        <property name="checkFormat" value="\.IndentationCheck$"/>
        <property name="offCommentFormat" value='^""".?'/>
        <property name="onCommentFormat" value='.'/>
    </module>

    <!-- ═══════════════════════════════════════════════════════════════
         TREE WALKER — per-token checks
    ═══════════════════════════════════════════════════════════════════ -->
    <module name="TreeWalker">

        <!-- Unchanged from Google base -->
        <module name="OuterTypeFilename"/>

        <!-- Unchanged from Google base -->
        <module name="MatchXpath">
            <property name="id" value="singleLineCommentStartWithSpace"/>
            <property name="query"
                      value="//SINGLE_LINE_COMMENT[./COMMENT_CONTENT[not(starts-with(@text, ' '))
                       and not(starts-with(@text, '/'))
                       and not(@text = '\n') and not(ends-with(@text, '//\n'))]]"/>
            <message key="matchxpath.match" value="''//'' must be followed by a whitespace."/>
        </module>

        <!-- Unchanged from Google base -->
        <module name="IllegalTokenText">
            <property name="tokens" value="STRING_LITERAL, CHAR_LITERAL, TEXT_BLOCK_CONTENT"/>
            <property name="format"
                      value="\\u00(08|09|0(a|A)|0(c|C)|0(d|D)|20|22|27|5(C|c))|\\(0(10|11|12|14|15|40|42|47)|134)"/>
            <property name="message"
                      value="Consider using special escape sequence instead of octal value or Unicode escaped value."/>
        </module>

        <!-- Unchanged from Google base -->
        <module name="AvoidEscapedUnicodeCharacters">
            <property name="allowEscapesForControlCharacters" value="true"/>
            <property name="allowByTailComment" value="true"/>
            <property name="allowNonPrintableEscapes" value="true"/>
        </module>

        <!-- Unchanged from Google base -->
        <module name="AvoidStarImport"/>
        <module name="OneTopLevelClass"/>
        <module name="NoLineWrap">
            <property name="tokens" value="PACKAGE_DEF, IMPORT, STATIC_IMPORT"/>
        </module>
        <module name="NeedBraces">
            <property name="tokens"
                      value="LITERAL_DO, LITERAL_ELSE, LITERAL_FOR, LITERAL_IF, LITERAL_WHILE"/>
        </module>

        <!-- Unchanged from Google base -->
        <module name="LeftCurly">
            <property name="id" value="LeftCurlyEol"/>
            <property name="tokens"
                      value="ANNOTATION_DEF, CLASS_DEF, CTOR_DEF, ENUM_CONSTANT_DEF, ENUM_DEF,
                    INTERFACE_DEF, LAMBDA, LITERAL_CATCH,
                    LITERAL_DO, LITERAL_ELSE, LITERAL_FINALLY, LITERAL_FOR, LITERAL_IF,
                    LITERAL_SWITCH, LITERAL_SYNCHRONIZED, LITERAL_TRY, LITERAL_WHILE, METHOD_DEF,
                    OBJBLOCK, STATIC_INIT, RECORD_DEF, COMPACT_CTOR_DEF, SWITCH_RULE"/>
        </module>
        <module name="LeftCurly">
            <property name="id" value="LeftCurlyNl"/>
            <property name="option" value="nl"/>
            <property name="tokens" value="LITERAL_CASE, LITERAL_DEFAULT"/>
        </module>
        <module name="SuppressionXpathSingleFilter">
            <property name="id" value="LeftCurlyNl"/>
            <property name="query" value="//SWITCH_RULE/SLIST"/>
        </module>

        <!-- Unchanged from Google base -->
        <module name="RightCurly">
            <property name="id" value="RightCurlySame"/>
            <property name="tokens"
                      value="LITERAL_TRY, LITERAL_CATCH, LITERAL_IF, LITERAL_ELSE, LITERAL_DO"/>
        </module>
        <module name="SuppressionXpathSingleFilter">
            <property name="id" value="RightCurlySame"/>
            <property name="query" value="//RCURLY[parent::SLIST[parent::LITERAL_CATCH
                               and not(parent::LITERAL_CATCH/following-sibling::*)]]"/>
        </module>
        <module name="RightCurly">
            <property name="id" value="RightCurlyAlone"/>
            <property name="option" value="alone"/>
            <property name="tokens"
                      value="CLASS_DEF, METHOD_DEF, CTOR_DEF, LITERAL_FOR, LITERAL_WHILE, STATIC_INIT,
                    INSTANCE_INIT, ANNOTATION_DEF, ENUM_DEF, INTERFACE_DEF, RECORD_DEF,
                    COMPACT_CTOR_DEF, LITERAL_SWITCH, LITERAL_CASE, LITERAL_DEFAULT,
                    LITERAL_FINALLY, LITERAL_CATCH"/>
        </module>
        <module name="SuppressionXpathSingleFilter">
            <property name="id" value="RightCurlyAlone"/>
            <property name="query" value="//RCURLY[parent::SLIST[count(./*)=1
                               and not(parent::LITERAL_CATCH)]
                               or (preceding-sibling::*[last()][self::LCURLY]
                               and not(parent::SLIST/parent::LITERAL_CATCH))
                               or (parent::SLIST/parent::LITERAL_CATCH
                               and parent::SLIST/parent::LITERAL_CATCH/following-sibling::*)]"/>
        </module>

        <!-- Unchanged from Google base -->
        <module name="WhitespaceAfter">
            <property name="tokens"
                      value="COMMA, SEMI, TYPECAST, ELLIPSIS, LITERAL_YIELD, LITERAL_CASE, ANNOTATIONS"/>
        </module>
        <module name="WhitespaceAround">
            <property name="allowEmptyConstructors" value="true"/>
            <property name="allowEmptyLambdas" value="true"/>
            <property name="allowEmptyMethods" value="true"/>
            <property name="allowEmptyTypes" value="true"/>
            <property name="allowEmptyLoops" value="true"/>
            <property name="allowEmptySwitchBlockStatements" value="true"/>
            <property name="ignoreEnhancedForColon" value="false"/>
            <property name="tokens"
                      value="ASSIGN, BAND, BAND_ASSIGN, BOR, BOR_ASSIGN, BSR, BSR_ASSIGN, BXOR,
                    BXOR_ASSIGN, COLON, DIV, DIV_ASSIGN, DO_WHILE, EQUAL, GE, GT, LAMBDA, LAND,
                    LCURLY, LE, LITERAL_CATCH, LITERAL_DO, LITERAL_ELSE, LITERAL_FINALLY,
                    LITERAL_FOR, LITERAL_IF, LITERAL_RETURN, LITERAL_SWITCH, LITERAL_SYNCHRONIZED,
                    LITERAL_TRY, LITERAL_WHILE, LOR, LT, MINUS, MINUS_ASSIGN, MOD, MOD_ASSIGN,
                    NOT_EQUAL, PLUS, PLUS_ASSIGN, QUESTION, RCURLY, SL, SLIST, SL_ASSIGN, SR,
                    SR_ASSIGN, STAR, STAR_ASSIGN, LITERAL_ASSERT,
                    TYPE_EXTENSION_AND, LITERAL_WHEN"/>
            <message key="ws.notFollowed"
                     value="WhitespaceAround: ''{0}'' is not followed by whitespace. Empty blocks
               may only be represented as '{}' when not part of a multi-block statement (4.1.3)"/>
            <message key="ws.notPreceded"
                     value="WhitespaceAround: ''{0}'' is not preceded with whitespace."/>
        </module>
        <module name="SuppressionXpathSingleFilter">
            <property name="checks" value="WhitespaceAround"/>
            <property name="query" value="//*[self::LITERAL_IF or self::LITERAL_ELSE or
                                 self::STATIC_INIT]/SLIST[count(./*)=1]
                                 | //*[self::STATIC_INIT or self::LITERAL_TRY or self::LITERAL_IF]
                                 //*[self::RCURLY][parent::SLIST[count(./*)=1]]
                                 | //SLIST[count(./*)=1][parent::LITERAL_TRY and
                                 not(following-sibling::*)]
                                 | //SLIST[count(./*)=1][parent::LITERAL_CATCH and
                                 not(parent::LITERAL_CATCH/following-sibling::*)]"/>
        </module>
        <module name="RegexpSinglelineJava">
            <property name="format" value="\{[ ]+\}"/>
            <property name="message" value="Empty blocks should have no spaces."/>
        </module>

        <!-- Unchanged from Google base -->
        <module name="OneStatementPerLine"/>
        <module name="MultipleVariableDeclarations"/>
        <module name="ArrayTypeStyle"/>
        <module name="MissingSwitchDefault"/>
        <module name="FallThrough"/>
        <module name="UpperEll"/>
        <module name="ModifierOrder"/>

        <!--
          [REMOVED] TextBlockGoogleStyleFormatting
          Requires Checkstyle 10.18+. Spring Boot 3.3's maven-checkstyle-plugin
          resolves to Checkstyle 10.12.x where this module does not exist and
          causes: "Unable to instantiate module TextBlockGoogleStyleFormatting".
          Re-enable when the project pins maven-checkstyle-plugin to 3.4.0+
          with an explicit <checkstyle.version>10.18.x</checkstyle.version>.
        -->

        <!-- Unchanged from Google base -->
        <module name="EmptyLineSeparator">
            <property name="tokens"
                      value="PACKAGE_DEF, IMPORT, STATIC_IMPORT, CLASS_DEF, INTERFACE_DEF, ENUM_DEF,
                    STATIC_INIT, INSTANCE_INIT, METHOD_DEF, CTOR_DEF, VARIABLE_DEF, RECORD_DEF,
                    COMPACT_CTOR_DEF"/>
            <property name="allowNoEmptyLineBetweenFields" value="true"/>
            <property name="allowMultipleEmptyLines" value="false"/>
        </module>

        <!-- Unchanged from Google base -->
        <module name="SeparatorWrap">
            <property name="id" value="SeparatorWrapDot"/>
            <property name="tokens" value="DOT"/>
            <property name="option" value="nl"/>
        </module>
        <module name="SeparatorWrap">
            <property name="id" value="SeparatorWrapComma"/>
            <property name="tokens" value="COMMA"/>
            <property name="option" value="EOL"/>
        </module>
        <module name="SeparatorWrap">
            <property name="id" value="SeparatorWrapEllipsis"/>
            <property name="tokens" value="ELLIPSIS"/>
            <property name="option" value="EOL"/>
        </module>
        <module name="SeparatorWrap">
            <property name="id" value="SeparatorWrapArrayDeclarator"/>
            <property name="tokens" value="ARRAY_DECLARATOR"/>
            <property name="option" value="EOL"/>
        </module>
        <module name="SeparatorWrap">
            <property name="id" value="SeparatorWrapMethodRef"/>
            <property name="tokens" value="METHOD_REF"/>
            <property name="option" value="nl"/>
        </module>

        <!-- Unchanged from Google base -->
        <module name="PackageName">
            <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
            <message key="name.invalidPattern"
                     value="Package name ''{0}'' must match pattern ''{1}''."/>
        </module>
        <module name="TypeName">
            <property name="format" value="^[A-Z][a-zA-Z0-9]*(?:[0-9](?:_[0-9]+)*)?$"/>
            <property name="tokens" value="CLASS_DEF"/>
            <message key="name.invalidPattern"
                     value="Type name ''{0}'' must match pattern ''{1}''."/>
        </module>
        <module name="TypeName">
            <property name="tokens" value="INTERFACE_DEF, ENUM_DEF, ANNOTATION_DEF, RECORD_DEF"/>
            <message key="name.invalidPattern"
                     value="Type name ''{0}'' must match pattern ''{1}''."/>
        </module>

        <!--
          [REMOVED] GoogleNonConstantFieldName
          This module exists only in Google's internal Checkstyle distribution.
          It is not present in the OSS release on Maven Central and causes:
          "Unable to instantiate module GoogleNonConstantFieldName"
          when running via maven-checkstyle-plugin with the standard artifact.

          [ADDED] MemberName — replaces GoogleNonConstantFieldName.
          Enforces lowerCamelCase on non-constant instance fields, which is
          the behaviour GoogleNonConstantFieldName enforced internally.
          The format intentionally excludes single-character names (^[a-z][a-z0-9])
          to match Google's stricter naming requirement (min 2 chars).
        -->
        <module name="MemberName">
            <property name="format" value="^[a-z][a-z0-9][a-zA-Z0-9]*$"/>
            <message key="name.invalidPattern"
                     value="Member name ''{0}'' must match pattern ''{1}''."/>
        </module>

        <!-- Unchanged from Google base -->
        <module name="ParameterName">
            <property name="format" value="^[a-z]([a-z0-9][a-zA-Z0-9]*)?$"/>
            <message key="name.invalidPattern"
                     value="Parameter name ''{0}'' must match pattern ''{1}''."/>
        </module>
        <module name="LambdaParameterName">
            <property name="format" value="^(_|[a-z]([a-z0-9][a-zA-Z0-9]*)?)$"/>
            <message key="name.invalidPattern"
                     value="Lambda parameter name ''{0}'' must match pattern ''{1}''."/>
        </module>
        <module name="CatchParameterName">
            <property name="format" value="^(_|[a-z]([a-z0-9][a-zA-Z0-9]*)?)$"/>
            <message key="name.invalidPattern"
                     value="Catch parameter name ''{0}'' must match pattern ''{1}''."/>
        </module>
        <module name="LocalVariableName">
            <property name="format" value="^(_|[a-z]([a-z0-9][a-zA-Z0-9]*)?)$"/>
            <message key="name.invalidPattern"
                     value="Local variable name ''{0}'' must match pattern ''{1}''."/>
        </module>
        <module name="PatternVariableName">
            <property name="format" value="^(_|[a-z]([a-z0-9][a-zA-Z0-9]*)?)$"/>
            <message key="name.invalidPattern"
                     value="Pattern variable name ''{0}'' must match pattern ''{1}''."/>
        </module>
        <module name="ClassTypeParameterName">
            <property name="format" value="(^[A-Z][0-9]?)$|([A-Z][a-zA-Z0-9]*[T]$)"/>
            <message key="name.invalidPattern"
                     value="Class type name ''{0}'' must match pattern ''{1}''."/>
        </module>
        <module name="RecordComponentName">
            <property name="format" value="^[a-z]([a-z0-9][a-zA-Z0-9]*)?$"/>
            <message key="name.invalidPattern"
                     value="Record component name ''{0}'' must match pattern ''{1}''."/>
        </module>
        <module name="RecordTypeParameterName">
            <property name="format" value="(^[A-Z][0-9]?)$|([A-Z][a-zA-Z0-9]*[T]$)"/>
            <message key="name.invalidPattern"
                     value="Record type name ''{0}'' must match pattern ''{1}''."/>
        </module>
        <module name="MethodTypeParameterName">
            <property name="format" value="(^[A-Z][0-9]?)$|([A-Z][a-zA-Z0-9]*[T]$)"/>
            <message key="name.invalidPattern"
                     value="Method type name ''{0}'' must match pattern ''{1}''."/>
        </module>
        <module name="InterfaceTypeParameterName">
            <property name="format" value="(^[A-Z][0-9]?)$|([A-Z][a-zA-Z0-9]*[T]$)"/>
            <message key="name.invalidPattern"
                     value="Interface type name ''{0}'' must match pattern ''{1}''."/>
        </module>

        <!-- Unchanged from Google base -->
        <module name="NoFinalizer"/>
        <module name="GenericWhitespace">
            <message key="ws.followed"
                     value="GenericWhitespace ''{0}'' is followed by whitespace."/>
            <message key="ws.preceded"
                     value="GenericWhitespace ''{0}'' is preceded with whitespace."/>
            <message key="ws.illegalFollow"
                     value="GenericWhitespace ''{0}'' should followed by whitespace."/>
            <message key="ws.notPreceded"
                     value="GenericWhitespace ''{0}'' is not preceded with whitespace."/>
        </module>

        <!--
          [CHANGED] Indentation: 2-space (Google) → 4-space.
          Rationale: the broader Java ecosystem, Spring Boot itself, the JDK
          source, and IntelliJ's default Java formatter all use 4 spaces.
          Adopting Google's 2-space style requires every contributor to
          reconfigure their IDE and produces merge conflicts with library
          code copied or adapted from other sources.

          Ratio relationships are preserved:
            throwsIndent = 2× basicOffset (was 4 = 2×2, now 8 = 2×4)
            lineWrappingIndentation = 2× basicOffset (same ratio)
            braceAdjustment changed 2→0: with 4-space indent, braces sit
            at the containing block's indentation level, not indented further.
        -->
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
            <property name="braceAdjustment" value="0"/>
            <property name="caseIndent" value="4"/>
            <property name="throwsIndent" value="8"/>
            <property name="lineWrappingIndentation" value="8"/>
            <property name="arrayInitIndent" value="4"/>
        </module>

        <!--
          [CHANGED] allowedAbbreviationLength: 0 → 2.
          Google bans all consecutive uppercase (ID must be Id, URL must be Url).
          Spring Boot teams routinely use DTO, SLA, JWT, URL as type suffixes.
          allowedAbbreviationLength=2 allows up to 2 consecutive uppercase
          (e.g. "ID" is OK, "DTO" still violates — use "Dto").
          If the team uses DTO/SLA/JWT as-is, raise this to 3.
          All other tokens unchanged from Google base.
        -->
        <module name="AbbreviationAsWordInName">
            <property name="ignoreFinal" value="false"/>
            <property name="allowedAbbreviationLength" value="2"/>
            <property name="tokens"
                      value="CLASS_DEF, INTERFACE_DEF, ENUM_DEF, ANNOTATION_DEF, ANNOTATION_FIELD_DEF,
                    PARAMETER_DEF, VARIABLE_DEF, METHOD_DEF, PATTERN_VARIABLE_DEF, RECORD_DEF,
                    RECORD_COMPONENT_DEF"/>
        </module>

        <!-- Unchanged from Google base -->
        <module name="NoWhitespaceBeforeCaseDefaultColon"/>
        <module name="OverloadMethodsDeclarationOrder"/>

        <!--
          [REMOVED] ConstructorsDeclarationGrouping
          Added in Checkstyle 10.17. Spring Boot 3.3's maven-checkstyle-plugin
          resolves to 10.12.x. Re-enable when pinning to 10.17+.
        -->

        <!-- Unchanged from Google base -->
        <module name="VariableDeclarationUsageDistance"/>
<!--        <module name="CustomImportOrder">-->
<!--            <property name="sortImportsInGroupAlphabetically" value="true"/>-->
<!--            <property name="separateLineBetweenGroups" value="true"/>-->
<!--            <property name="customImportOrderRules" value="STATIC###THIRD_PARTY_PACKAGE"/>-->
<!--        </module>-->
        <module name="MethodParamPad">
            <property name="tokens"
                      value="CTOR_DEF, LITERAL_NEW, METHOD_CALL, METHOD_DEF, CTOR_CALL,
                    SUPER_CTOR_CALL, ENUM_CONSTANT_DEF, RECORD_DEF, RECORD_PATTERN_DEF"/>
        </module>
        <module name="NoWhitespaceBefore">
            <property name="tokens"
                      value="COMMA, SEMI, POST_INC, POST_DEC, DOT,
                    LABELED_STAT, METHOD_REF, ELLIPSIS"/>
            <property name="allowLineBreaks" value="true"/>
        </module>
        <module name="SuppressionXpathSingleFilter">
            <property name="checks" value="NoWhitespaceBefore"/>
            <property name="query"
                      value="//ELLIPSIS[preceding-sibling::TYPE/ANNOTATIONS[ANNOTATION[LPAREN]
              or not(following-sibling::*)]]"/>
        </module>
        <module name="ParenPad">
            <property name="tokens"
                      value="ANNOTATION, ANNOTATION_FIELD_DEF, CTOR_CALL, CTOR_DEF, DOT, ENUM_CONSTANT_DEF,
                    EXPR, LITERAL_CATCH, LITERAL_DO, LITERAL_FOR, LITERAL_IF, LITERAL_NEW,
                    LITERAL_SWITCH, LITERAL_SYNCHRONIZED, LITERAL_WHILE, METHOD_CALL,
                    METHOD_DEF, QUESTION, RESOURCE_SPECIFICATION, SUPER_CTOR_CALL, LAMBDA,
                    RECORD_DEF, RECORD_PATTERN_DEF"/>
        </module>
        <module name="OperatorWrap">
            <property name="id" value="OperatorWrapNL"/>
            <property name="option" value="NL"/>
            <property name="tokens"
                      value="BAND, BOR, BSR, BXOR, DIV, EQUAL, GE, GT, LAND, LE, LITERAL_INSTANCEOF, LOR,
                    LT, MINUS, MOD, NOT_EQUAL, PLUS, QUESTION, SL, SR, STAR, METHOD_REF,
                    TYPE_EXTENSION_AND "/>
        </module>
        <module name="OperatorWrap">
            <property name="id" value="OperatorWrapEOL"/>
            <property name="option" value="EOL"/>
            <property name="tokens" value="LAMBDA"/>
        </module>

        <!--
          ANNOTATION LOCATION — unchanged from Google base.
          TYPE/PACKAGE: one annotation per line, no parameterless same-line.
          METHOD/CTOR:  one annotation per line (default).
          VARIABLE:     multiple annotations on same line allowed — covers
                        @Valid @RequestBody CreateTicketRequest request.
        -->
        <module name="AnnotationLocation">
            <property name="id" value="AnnotationLocationTypeAndPackage"/>
            <property name="tokens"
                      value="CLASS_DEF, INTERFACE_DEF, ENUM_DEF, RECORD_DEF, PACKAGE_DEF"/>
            <property name="allowSamelineSingleParameterlessAnnotation" value="false"/>
        </module>
        <module name="AnnotationLocation">
            <property name="id" value="AnnotationLocationMethodsAndCtors"/>
            <property name="tokens" value="METHOD_DEF, CTOR_DEF, COMPACT_CTOR_DEF"/>
        </module>
        <module name="AnnotationLocation">
            <property name="id" value="AnnotationLocationVariables"/>
            <property name="tokens" value="VARIABLE_DEF"/>
            <property name="allowSamelineMultipleAnnotations" value="true"/>
        </module>

        <!--
          [REMOVED] MissingOverrideOnRecordAccessor — Checkstyle 10.14+ only.
        -->

        <!-- Unchanged from Google base -->
        <module name="NonEmptyAtclauseDescription"/>
        <module name="InvalidJavadocPosition"/>
        <module name="JavadocTagContinuationIndentation"/>
        <module name="SummaryJavadoc">
            <property name="forbiddenSummaryFragments"
                      value="^@return the *|^This method returns |^A [{]@code [a-zA-Z0-9]+[}]( is a )|^[a-z]"/>
        </module>
        <module name="JavadocParagraph">
            <property name="allowNewlineParagraph" value="false"/>
        </module>
        <module name="RequireEmptyLineBeforeBlockTagGroup"/>
        <module name="AtclauseOrder">
            <property name="tagOrder" value="@param, @return, @throws, @deprecated"/>
            <property name="target"
                      value="CLASS_DEF, INTERFACE_DEF, ENUM_DEF, METHOD_DEF, CTOR_DEF,
                    VARIABLE_DEF, RECORD_DEF, COMPACT_CTOR_DEF"/>
        </module>

        <!--
          [REMOVED] JavadocLeadingAsteriskAlign  — Checkstyle 10.18+ only.
          [REMOVED] JavadocMissingLeadingAsterisk — Checkstyle 10.18+ only.
        -->

        <!-- Unchanged from Google base -->
        <module name="JavadocMethod">
            <property name="accessModifiers" value="public"/>
            <property name="allowMissingParamTags" value="true"/>
            <property name="allowMissingReturnTag" value="true"/>
            <property name="allowedAnnotations" value="Override, Test"/>
            <property name="tokens"
                      value="METHOD_DEF, CTOR_DEF, ANNOTATION_FIELD_DEF, COMPACT_CTOR_DEF"/>
        </module>

        <!--
          [CHANGED] MissingJavadocMethod: scope protected → private.
          Google requires Javadoc on all protected+ methods. In Spring Boot,
          protected methods on @Service and @Repository classes are
          implementation details wired by the DI container — not public API.
          Requiring Javadoc on them generates hundreds of meaningless stubs.
          scope=private means ONLY explicitly public methods need Javadoc,
          which aligns with what teams actually review and maintain.
        -->
<!--        <module name="MissingJavadocMethod">-->
<!--            <property name="scope" value="private"/>-->
<!--            <property name="allowMissingPropertyJavadoc" value="true"/>-->
<!--            <property name="allowedAnnotations" value="Override, Test"/>-->
<!--            <property name="tokens"-->
<!--                      value="METHOD_DEF, CTOR_DEF, ANNOTATION_FIELD_DEF, COMPACT_CTOR_DEF"/>-->
<!--        </module>-->
<!--        <module name="SuppressionXpathSingleFilter">-->
<!--            <property name="checks" value="MissingJavadocMethod"/>-->
<!--            <property name="query" value="//*[self::METHOD_DEF or self::CTOR_DEF-->
<!--                                 or self::ANNOTATION_FIELD_DEF or self::COMPACT_CTOR_DEF]-->
<!--                                 [ancestor::*[self::INTERFACE_DEF or self::CLASS_DEF-->
<!--                                 or self::RECORD_DEF or self::ENUM_DEF]-->
<!--                                 [not(./MODIFIERS/LITERAL_PUBLIC)]]"/>-->
<!--        </module>-->

        <!--
          [CHANGED] MissingJavadocType: scope protected → private.
          Same rationale as MissingJavadocMethod. @Service implementations,
          @Repository classes, and @Configuration beans are wired by Spring —
          they are not public API and do not benefit from mandatory Javadoc.
        -->
<!--        <module name="MissingJavadocType">-->
<!--            <property name="scope" value="private"/>-->
<!--            <property name="tokens"-->
<!--                      value="CLASS_DEF, INTERFACE_DEF, ENUM_DEF, RECORD_DEF, ANNOTATION_DEF"/>-->
<!--            <property name="excludeScope" value="nothing"/>-->
<!--        </module>-->

        <!-- Unchanged from Google base — includes test method name suppression -->
        <module name="MethodName">
            <property name="format"
                      value="^(?![a-z]$)(?![a-z][A-Z])[a-z][a-z0-9]*(?:[A-Z][a-z0-9]*)*(?:_[0-9]+)*$"/>
            <message key="name.invalidPattern"
                     value="Method name ''{0}'' must match pattern ''{1}''."/>
        </module>
        <module name="SuppressionXpathSingleFilter">
            <property name="checks" value="MethodName"/>
            <property name="query" value="//METHOD_DEF[
                                     ./MODIFIERS/ANNOTATION//IDENT[contains(@text, 'Test')]
                                   ]/IDENT"/>
            <property name="message"
                      value="'[a-z][a-z0-9][a-zA-Z0-9]*(?:_[a-z][a-z0-9][a-zA-Z0-9]*)*'"/>
        </module>

        <!-- Unchanged from Google base -->
        <module name="SingleLineJavadoc"/>
        <module name="TodoComment">
            <property name="id" value="TodoFormat"/>
            <property name="format" value="^[ \t]*(?!TODO:)(?i:TODO)\b:?"/>
            <message key="todo.match"
                     value="''TODO:'' must be written in all caps and followed by a colon."/>
        </module>
        <module name="TodoComment">
            <property name="id" value="BoxComments"/>
            <property name="format" value="^[ \t]*([*=#])\1{8,}[ \t]*$"/>
            <message key="todo.match"
                     value="Comment uses box-like repetitive character pattern."/>
        </module>
        <module name="EmptyCatchBlock">
            <property name="commentFormat" value="\w+"/>
        </module>
        <module name="CommentsIndentation">
            <property name="tokens" value="SINGLE_LINE_COMMENT, BLOCK_COMMENT_BEGIN"/>
        </module>

        <!-- ═══════════════════════════════════════════════════════════
             [ADDED] Rules not present in google_checks.xml
        ═══════════════════════════════════════════════════════════════ -->

        <!--
          [ADDED] MethodLength — max 50 lines, blank lines excluded.
          Google has no method length limit. We enforce 50 executable lines
          because Spring Boot service methods exceeding this are an almost
          certain SRP violation. Blank lines don't contribute to complexity.
          Suppress with @SuppressWarnings("checkstyle:MethodLength") where
          a long switch on a large enum is genuinely unavoidable.
        -->
        <module name="MethodLength">
            <property name="max" value="50"/>
            <property name="countEmpty" value="false"/>
        </module>

        <!--
          [ADDED] CyclomaticComplexity — max 15.
          Google has no complexity limit. CC > 15 requires 15+ test cases
          for full branch coverage — a practical forcing function to decompose.
          15 is more lenient than the textbook ideal of 10 to accommodate
          Spring status-transition methods that branch on enum values.
        -->
        <module name="CyclomaticComplexity">
            <property name="max" value="15"/>
        </module>

        <!-- ═══════════════════════════════════════════════════════════
             Suppression infrastructure — unchanged from Google base
        ═══════════════════════════════════════════════════════════════ -->
        <module name="SuppressionXpathFilter">
            <property name="file"
                      value="${org.checkstyle.google.suppressionxpathfilter.config}"
                      default="checkstyle-xpath-suppressions.xml"/>
            <property name="optional" value="true"/>
        </module>
        <module name="SuppressWarningsHolder"/>
        <module name="SuppressionCommentFilter">
            <property name="offCommentFormat" value="CHECKSTYLE.OFF\: ([\w\|]+)"/>
            <property name="onCommentFormat"  value="CHECKSTYLE.ON\: ([\w\|]+)"/>
            <property name="checkFormat"      value="$1"/>
        </module>
        <module name="SuppressWithNearbyCommentFilter">
            <property name="commentFormat"    value="CHECKSTYLE.SUPPRESS\: ([\w\|]+)"/>
            <property name="checkFormat"      value="$1"/>
            <property name="influenceFormat"  value="1"/>
        </module>

    </module>
</module>